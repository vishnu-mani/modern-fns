# modern-fns

All notable changes to this project are documented here.

This project follows [semantic versioning](https://semver.org/). Entries below are generated
from [changesets](https://github.com/changesets/changesets) when a release is versioned — add
one with `npm run changeset` rather than editing this file by hand.

<!-- Released versions are prepended here by `changeset version`. -->
