# modern-fns node demo

A runnable tour of the three import styles and the utilities that do the most work in real
applications.

```bash
npm install     # links the local package via file:../..
npm start
```

The same code runs unchanged in Vite, Nuxt, Next, Svelte or a plain browser bundle — none of
these utilities touch Node built-ins.
